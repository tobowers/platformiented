require 'spec_helper'

describe Platformiented do
  it 'has a version number' do
    expect(Platformiented::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
